const cipher = {


  /*deseja decifrar ou cifrar ? OK
  se for cifrar então qual offset ?
  digite a frase que deseja cifrar
  recebe a frase
  converçã da frase (atribui valor para cada letr do alfabeto a=1, b=2 ...  ; 
  pega o valor offset concatena com o valor de cada letra quando chegar no w 
  dependendo do valor da soma do offset (exemplo 27) o w começa a valer 1 = A e assim por diante...
  guardar a primeira letra numa variável frase = frase + a letra nova */
 

};

export default cipher;